<p id="notice"><%= notice %></p>

<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task %>｜
      <%= link_to '削除', task, method: :delete, data: { confirm: '削除してもよかですか?' } %>
    <% end %>
</ul>

<hr>
<h2>タスク登録</h2>

<%= render 'form', task: @task %>


